name: Run test suite

on:
    pull_request:

jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - name: Check typescript
              run: "make check"
            - name: Check linter
              run: "make eslint"
            - name: Run tests
              run: "make test"
